---

---

<div class="main-wrap">
  <img
    alt="Agricultural inputs distribution"
    srcset="
    /images/quote_400.webp
    400w,
    /images/quote_700.webp
    700w,
    /images/quote_1200.webp
    1200w,
    /images/quote.webp
    1400w"
    sizes="100vmin"
  />
  <div class="content">
    <p class="quote">
      Provision of required services to destitute people is not a task of
      charity, rather an act of humanity and justice.
    </p>
    <footer>- Salah Y. Majid</footer>
  </div>
</div>

<style>
  .main-wrap {
    --bg-filter-color: var(--neutral20);
    --bg-filter-opacity: 0.7;
    display: grid;
    place-items: center;
    background: var(--surface);
    grid-template-columns: repeat(2, 1fr);
    max-width: 100%;
    height: 30rem;
    position: relative;
    isolation: isolate;
  }

  img,
  .content {
    grid-row: 1/2;
  }

  img {
    grid-column: 1 / -1;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .content {
    grid-column: 1 / -1;
    z-index: 1;
  }

  .main-wrap::after {
    content: "";
    position: absolute;
    inset: 0;
    background: var(--bg-filter-color);
    opacity: var(--bg-filter-opacity);
  }

  .quote {
    font-size: var(--fs-fl-sm);
    padding-block: 0.5rem;
    padding-inline: 1.25rem;
    line-height: 1.3;
    max-width: 60ch;
    color: var(--on-surface-dark);
  }

  .quote::before {
    content: open-quote;
    display: inline;
    line-height: 0;
    left: -0.1em;
    position: relative;
    top: 0.5em;
    color: var(--primary);
    font-size: 3em;
  }
  .quote::after {
    content: close-quote;
    display: inline;
    line-height: 0;
    left: 0.1em;
    position: relative;
    top: 0.5em;
    color: var(--primary);
    font-size: 3em;
  }
  footer {
    margin-inline-end: 1rem;
    text-align: right;
    font-size: var(--fs-fl-sm);
    font-style: italic;
    color: var(--on-surface-dark);
  }
</style>
